<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <style>
        .font-display { font-family: 'Fredoka One', cursive; }
        .font-body { font-family: 'Nunito', sans-serif; }
    </style>
</head>
<body class="bg-gray-100 min-h-screen font-body text-gray-800 p-8">
    <div class="max-w-2xl mx-auto bg-white p-8 rounded-xl shadow-md border-t-8 border-blue-600">
        <div class="flex justify-between items-center mb-6 border-b pb-4">
            <h1 class="text-3xl font-display uppercase text-blue-600">Edit Art Piece</h1>
            <a href="/admin" class="text-gray-500 hover:text-black font-bold uppercase tracking-wider">&larr; Back to Dashboard</a>
        </div>

        <form action="/admin/edit/<%= item.id %>" method="POST" class="space-y-6">
            <div>
                <label class="block text-gray-700 font-bold text-sm uppercase mb-2">Item Name</label>
                <input type="text" name="name" value="<%= item.name %>" required class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-blue-600 outline-none bg-gray-50">
            </div>
            
            <div>
                <label class="block text-gray-700 font-bold text-sm uppercase mb-2">Description</label>
                <textarea name="description" rows="4" required class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-blue-600 outline-none bg-gray-50"><%= item.description %></textarea>
            </div>

            <div class="bg-gray-50 p-4 rounded-xl border border-gray-200">
                <label class="block text-gray-700 font-bold text-sm uppercase mb-2">Bidding Rules (Tier)</label>
                <select name="bidType" class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-blue-600 outline-none font-bold text-blue-700">
                    <option value="1" <%= item.bid_type === 1 ? 'selected' : '' %>>Tier 1 (Min Inc: $0.25 | Max Inc: $1.00)</option>
                    <option value="2" <%= item.bid_type === 2 ? 'selected' : '' %>>Tier 2 (Min Inc: $0.50 | Max Inc: $5.00)</option>
                    <option value="3" <%= item.bid_type === 3 ? 'selected' : '' %>>Tier 3 (Min Inc: $1.00 | Max Inc: $10.00)</option>
                    <option value="4" <%= item.bid_type === 4 ? 'selected' : '' %>>Tier 4 (Min Inc: $1.00 | Max Inc: $25.00)</option>
                </select>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="bg-gray-50 p-4 rounded-xl border border-gray-200">
                    <label class="block text-gray-700 font-bold text-sm uppercase mb-2">Starting Price ($)</label>
                    <input type="number" step="0.01" name="startPrice" value="<%= Number(item.start_price).toFixed(2) %>" required class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-blue-600 outline-none">
                </div>
                
                <div class="bg-gray-50 p-4 rounded-xl border border-gray-200">
                    <label class="block text-gray-700 font-bold text-sm uppercase mb-2">Placement Order</label>
                    <input type="number" name="placement" value="<%= item.placement || 0 %>" class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-blue-600 outline-none" placeholder="0">
                </div>
            </div>
            
            <div class="mt-8 pt-4">
                <button type="submit" class="w-full bg-blue-600 text-white p-4 rounded-xl hover:bg-blue-700 font-bold text-xl shadow-md transition transform active:scale-95 uppercase tracking-widest">
                    Save Changes
                </button>
            </div>
        </form>
    </div>
</body>
</html>